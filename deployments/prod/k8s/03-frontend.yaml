apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-prod
  namespace: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-prod
  template:
    metadata:
      labels:
        app: frontend-prod
    spec:
      containers:
      - name: frontend
        image: 192.168.1.184/3tier/frontend:prod-latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-prod-service
  namespace: prod
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30082
  selector:
    app: frontend-prod
