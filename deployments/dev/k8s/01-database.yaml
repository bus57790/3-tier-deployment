apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-prod
  namespace: prod
spec:
  serviceName: postgres-prod
  replicas: 1
  selector:
    matchLabels:
      app: postgres-prod
  template:
    metadata:
      labels:
        app: postgres-prod
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_DB
          value: app_prod
        - name: POSTGRES_USER
          value: prod_admin
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-prod-secret
              key: password
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-prod-service
  namespace: prod
spec:
  ports:
  - port: 5432
  selector:
    app: postgres-prod
